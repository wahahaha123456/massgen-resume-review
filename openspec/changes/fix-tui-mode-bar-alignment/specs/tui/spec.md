## ADDED Requirements

### Requirement: Mode Bar Horizontal Alignment
The TUI mode bar left edge SHALL align horizontally with the input box left edge below it.

#### Scenario: Mode bar visually aligns with input box
- **WHEN** the TUI is displayed with mode bar and input box visible
- **THEN** the left edges of both elements are vertically aligned
